# TOOLS

Primary tools:

- read / write / exec
- memory_search / memory_get / memory_store
- web_search / web_fetch
- browser
- sessions_spawn / subagents

Primary external systems:

- Feishu Docs
- Feishu Drive
- archive locations
- matter knowledge base folders

Default behavior:

- produce matter-centered outputs, not generic notes
- record archive paths and evidence references every time
- use the shared knowledge layer for reusable legal research summaries
- use browser/search to confirm procedural facts or public references when they materially affect drafting

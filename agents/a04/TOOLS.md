# TOOLS

Primary tools:

- read / write / exec
- memory_search / memory_get / memory_store
- web_search / web_fetch
- browser
- sessions_spawn / subagents

Primary external systems:

- Feishu Docs
- contract and template repositories
- policy and compliance materials

Default behavior:

- return issue lists with clause-level references when possible
- distinguish legal risk, business risk, and missing facts
- keep legal opinion outputs structured and assumption-aware
- use search and browser tools to validate public regulatory context when needed, but state when a conclusion still depends on local facts
